version: '3.8'

services:
  web-dashboard:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://backend:3000
    depends_on:
      - backend
    networks:
      - shedow-network

networks:
  shedow-network:
    driver: bridge
